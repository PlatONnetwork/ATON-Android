<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="16dp"
    android:paddingBottom="16dp">

    <TextView
        android:id="@+id/tv_community_url"
        style="@style/Text.61646e.13"
        android:layout_width="0dp"
        android:ellipsize="end"
        android:gravity="left|center_vertical"
        android:lines="1"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/iv_copy"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="https://t.me/PlatONHK" />

    <ImageView
        android:id="@+id/iv_jump"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:src="@drawable/icon_jump"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_copy"
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_marginRight="20dp"
        android:src="@drawable/icon_copy_text"
        app:layout_constraintBottom_toBottomOf="@id/iv_jump"
        app:layout_constraintRight_toLeftOf="@id/iv_jump"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/iv_qr_code"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="16dp"
        android:src="@drawable/icon_twitter_community"
        android:visibility="gone"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_community_url" />

</android.support.constraint.ConstraintLayout>